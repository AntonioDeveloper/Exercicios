Calcule o seu IMC

const nome = "Carlos";
const peso = 80;
const altura = 1.76;
let imc = peso / (altura * altura);

document.write(imc);

if(imc >= 30){
  document.write(`<br> ${nome} você está acima do peso`);
} else {
  document.write(`<br> ${nome} você não está acima do peso`);
}

